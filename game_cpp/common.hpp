#pragma once

namespace params
{
    namespace ship
    {
        constexpr float LINEAR_SPEED = 0.5f;
        constexpr float ANGULAR_SPEED = 0.5f;
    }

    namespace aircraft
    {
        constexpr size_t AIRCRAFT_NUM = 5;
        constexpr float LINEAR_ACCELERATION = 1.0f;
        constexpr float LINEAR_SPEED = 2.f;
        constexpr float ANGULAR_SPEED = 2.5f;
        constexpr float FLIGHT_TIME = 10.0f;
        constexpr float DELAY_TIME = 2.0f;    
    }
}